"""
Generator for modernizing node-4-1 through node-4-35 into genuine Web Architecture,
HTTP Protocol, Asynchronous Event Loops, FastAPI, SSE, WebSockets & Distributed Caching.
"""
import json

P4_LESSONS = [
    (1, "The Modern Web Protocol Stack: Application vs Transport Layer",
     "Deep dive into the 5-layer Internet model (L3 IP, L4 TCP/UDP, L7 Application).",
     ["Client-server communications", "Socket abstractions", "Transport trade-offs", "Packet delivery invariants"],
     "Confusing transport layer reliability (TCP) with application layer idempotency (HTTP).",
     "Inspect raw socket packets and correctly differentiate TCP state transitions from HTTP request lifetimes.",
     "GatewayAI: Core socket network transport architecture."),

    (2, "HTTP/1.1 vs HTTP/2 (Multiplexing, HPACK) vs HTTP/3 (QUIC, UDP)",
     "Evolution of web protocols: head-of-line blocking, binary framing, and UDP 0-RTT handshakes.",
     ["HTTP/1.1 pipelining limitations", "HTTP/2 binary streams and HPACK header compression", "QUIC UDP connection migration", "0-RTT connection resumption"],
     "Assuming HTTP/2 eliminates all head-of-line blocking while ignoring TCP-level packet loss stalls.",
     "Compare request multiplexing performance between HTTP/1.1 and HTTP/2 under artificial packet loss.",
     "GatewayAI: High-throughput HTTP multiplexing layer."),

    (3, "DNS Resolution, Record Types, Anycast & Edge Routing",
     "How domain names map to distributed IPs: root servers, TLDs, recursive resolvers, and TTL caching.",
     ["DNS lookup lifecycle: A, AAAA, CNAME, TXT, SRV records", "Authoritative vs recursive resolvers", "TTL cache invalidation and stale cache hazards", "Anycast BGP routing across distributed edge datacenters"],
     "Setting excessively long DNS TTLs during production domain migrations, locking users out during failover.",
     "Trace full recursive DNS resolution and calculate propagation delays across multi-tier caches.",
     "Cloudflare & AWS Route53 architecture."),

    (4, "TLS 1.3 Cryptography: Key Exchange, Certificates & Forward Secrecy",
     "Modern web transport security: ECDHE asymmetric key exchange, AES-GCM symmetric encryption, and PKI.",
     ["TLS 1.3 single round-trip (1-RTT) handshake", "Elliptic-Curve Diffie-Hellman Ephemeral (ECDHE) key exchange", "X.509 certificate chains, Certificate Authorities (CA), and OCSP stapling", "Perfect Forward Secrecy (PFS) and session ticket resumption"],
     "Storing private keys in application repos or omitting root CA verification in HTTP client configs.",
     "Validate a TLS certificate chain and compute shared session keys using ECDHE.",
     "Production mTLS and API gateway security."),

    (5, "REST Architectural Constraints & Resource URI Modeling",
     "The 6 architectural constraints of REST: statelessness, client-server, cacheability, uniform interface, layered system, code-on-demand.",
     ["Resource-oriented URI design: nouns vs verbs, pluralization, hierarchy", "HATEOAS and hypermedia state transitions", "Statelessness: eliminating server-side session affinity", "Representation decoupled from storage models"],
     "Embedding RPC verbs in REST URIs (e.g. `/getUserData?id=1`) violating uniform interface constraints.",
     "Design a clean RESTful URI hierarchy for a multi-tenant AI prompt library.",
     "PromptAPI: Core REST resource schema."),

    (6, "HTTP Verbs & Idempotency RFC 9110 Standards",
     "Safe vs Idempotent HTTP methods: GET, HEAD, OPTIONS, POST, PUT, PATCH, DELETE.",
     ["Safe methods: GET, HEAD, OPTIONS (read-only, zero side effects)", "Idempotent methods: PUT, DELETE, GET (repeatable without cumulative side effects)", "Non-idempotent operations: POST and state mutation", "RFC 9110 compliance for network retry safety"],
     "Implementing non-idempotent mutations on GET requests, triggering duplicate billing on web crawler scans.",
     "Prove idempotency of a PUT batch update under 5 duplicate network re-transmissions.",
     "GatewayAI: Payment and credit deduction safety."),

    (7, "Status Codes Precision & Client Error Recovery",
     "Standardized HTTP response codes: 2xx Success, 3xx Redirection, 4xx Client Errors, 5xx Server Errors.",
     ["200 OK vs 201 Created vs 204 No Content vs 202 Accepted", "400 Bad Request vs 422 Unprocessable Entity", "401 Unauthorized (unauthenticated) vs 403 Forbidden (insufficient permissions)", "429 Too Many Requests (with Retry-After headers) vs 503 Service Unavailable"],
     "Returning 200 OK with `{\"error\": \"failed\"}` in body, preventing upstream proxies from retrying.",
     "Map all API error conditions to RFC-standard status codes with structured Problem Details (RFC 7807).",
     "Enterprise API error standardization."),

    (8, "Content Negotiation, Headers & MIME Types",
     "Client-server metadata exchange: Accept, Content-Type, Content-Encoding, Cache-Control, and CORS headers.",
     ["MIME media types: `application/json`, `text/event-stream`, `multipart/form-data`", "Accept header quality factors (`q=0.9`) and proactive negotiation", "Compression: gzip, Brotli (`br`), and streaming decompression", "Header security: Strict-Transport-Security, X-Content-Type-Options"],
     "Omitting `Content-Type: application/json` in request dispatches, causing servers to reject payloads.",
     "Implement dynamic content negotiation returning either JSON or YAML based on the Accept header.",
     "Multi-format API gateway response serializer."),

    (9, "Asynchronous I/O Mechanics: Event Loops & Non-Blocking Sockets",
     "Why async beats thread-per-connection: non-blocking sockets, OS select/poll/epoll, and Python asyncio.",
     ["Synchronous blocking I/O bottleneck: thread exhaustion under 10k connections", "OS non-blocking sockets and readiness notifications", "Event loop execution cycle: timer heap, I/O callbacks, microtask queue", "Cooperative coroutines: yielding control without thread context switching overhead"],
     "Executing a synchronous blocking sleep (`time.sleep`) inside an async event loop, freezing all concurrent users.",
     "Build a non-blocking asynchronous socket server handling 1,000 concurrent client pings without threads.",
     "High-concurrency streaming server foundation."),

    (10, "The Python ASGI Specification & Lifespan Protocol",
     "Asynchronous Server Gateway Interface (ASGI): decoupling web servers (Uvicorn) from frameworks (FastAPI).",
     ["WSGI synchronous limitations vs ASGI asynchronous duplex capability", "ASGI scope, receive, and send callables", "Handling HTTP requests vs WebSocket connections vs Lifespan events", "Application startup and shutdown hooks: connection pool warmup and graceful draining"],
     "Failing to close database connection pools during ASGI shutdown, causing orphaned connections in Postgres.",
     "Write a minimal raw ASGI application from scratch without any framework.",
     "FastAPI and Uvicorn runtime internals."),

    (11, "Uvicorn Architecture: Workers, Sockets & Gunicorn Process Managers",
     "Production server topologies: running multi-worker Uvicorn under Gunicorn process supervisors.",
     ["Single-process event loop vs multi-process worker concurrency", "Gunicorn master process: signal handling (HUP, TERM), worker recycling, and heartbeats", "Worker count heuristics: `(2 * CPU_CORES) + 1` for I/O bound workloads", "Socket reuse (`SO_REUSEPORT`) and kernel load balancing across workers"],
     "Spawning 50 Uvicorn workers on a 2-core cloud VM, inducing thrashing and memory exhaustion.",
     "Configure a production Gunicorn+Uvicorn deployment with automated worker restarts on memory thresholds.",
     "Production Docker deployment for GatewayAI."),

    (12, "FastAPI Core Mechanics: Declarative Routing & Request Context",
     "The architecture of FastAPI: path operations, APIRouter modularization, and automatic OpenAPI schema binding.",
     ["FastAPI application instance: mounting sub-routers, tags, and metadata", "Path operation decorators: `@app.get`, `@app.post`, response models", "Request context lifecycle: request state, client IP extraction, base URL resolution", "Modular project structure: separating routes, schemas, services, and dependencies"],
     "Declaring routes with synchronous `def` instead of `async def` when performing I/O, accidentally exhausting threadpools.",
     "Structure a clean multi-router FastAPI service with versioned API endpoints.",
     "PromptAPI: Core service routing hierarchy."),

    (13, "Request Body Parsing & Pydantic v2 Serialization Performance",
     "High-speed validation: how Pydantic v2 (compiled in Rust) validates incoming HTTP request payloads.",
     ["Pydantic BaseModel: field declarations, type coercion, and defaults", "Validation decorators: `@field_validator` and `@model_validator`", "Rust core (pydantic-core) throughput advantages over pure Python serialization", "Excluding unset/none fields during response serialization (`model_dump(exclude_unset=True)`)"],
     "Performing expensive database lookups inside a Pydantic field validator rather than in the service layer.",
     "Benchmark Pydantic v2 JSON parsing of a complex 100-field nested payload against standard library json.",
     "High-throughput request validation engine."),

    (14, "Path Parameters, Query Parameters & Header Validation",
     "Declarative parameter extraction: Path, Query, Header, Cookie, and Body metadata in FastAPI.",
     ["Path parameters with regex constraints and type coercion (`item_id: UUID`)", "Query parameters: optional filters, pagination offsets, default values", "Header extraction: API keys, client versions, and trace IDs", "Custom validation error responses: transforming 422 errors into friendly client messages"],
     "Allowing unbounded query parameter integers leading to out-of-memory errors on massive limit queries.",
     "Implement strict pagination and filtering parameters with custom validation constraints.",
     "Safe API input filtering."),

    (15, "FastAPI Dependency Injection System: Providers, Scopes & Yield Fixtures",
     "Hierarchical DI in FastAPI: `Depends()`, sub-dependencies, database sessions, and cleanup with yield.",
     ["What is Dependency Injection: inversion of control and test isolation", "`Depends` callables: resolving authentication, database connections, and configs", "Yield dependencies: context managers for transactions with automatic cleanup on response dispatch", "Class-based dependencies: stateful parameter resolution"],
     "Leaking database sessions by failing to use `yield` or failing to commit/rollback inside a try/finally block.",
     "Build a hierarchical dependency chain that extracts an API key, loads the tenant profile, and injects a scoped DB session.",
     "Enterprise multi-tenant dependency architecture."),

    (16, "Cross-Origin Resource Sharing (CORS): Preflight OPTIONS & Origin Policies",
     "Browser security sandbox: Same-Origin Policy (SOP), CORS headers, and preflight handshake mechanics.",
     ["Same-Origin Policy: protocol, domain, and port matching", "Simple requests vs preflight requests: when browsers send `OPTIONS`", "CORS response headers: `Access-Control-Allow-Origin`, `Methods`, `Headers`, `Credentials`", "The wildcard `*` security disaster: why wildcard origins break credentialed cookies"],
     "Configuring `allow_origins=['*']` with `allow_credentials=True`, creating a major cross-site security vulnerability.",
     "Configure production CORS middleware with strict whitelist matching and test preflight handling.",
     "Frontend-to-backend authentication safety."),

    (17, "Middleware Architecture: Request Interception, Timing & Correlation IDs",
     "The onion model: wrapping HTTP requests with ASGI middleware for observability, timing, and security.",
     ["ASGI middleware lifecycle: pre-processing request, passing to next, post-processing response", "Request correlation IDs: tracing requests across microservice hops (`X-Request-ID`)", "Timing headers: `Server-Timing` for measuring database vs computation latency", "Error catching middleware: handling unhandled exceptions before they drop the connection"],
     "Consuming the request body stream inside middleware without resetting it, causing downstream handlers to hang.",
     "Write a custom ASGI middleware that injects UUID correlation IDs and measures microsecond execution times.",
     "Observability middleware for GatewayAI."),

    (18, "Authentication I: Session Cookies vs Stateless JSON Web Tokens (JWT)",
     "Architectural trade-offs: stateful server-side sessions with Redis vs stateless cryptographically signed tokens.",
     ["Stateful sessions: session IDs, server store (Redis), instant revocation capability", "Stateless tokens: self-contained claims, horizontal scalability, zero database lookup requirement", "Token revocation dilemma: why blacklisting stateless tokens reintroduces stateful lookups", "Security trade-offs: CSRF risk on cookies vs XSS risk on localStorage tokens"],
     "Storing sensitive user passwords or internal database keys inside unencrypted JWT claims.",
     "Demonstrate token verification without database queries and analyze token invalidation strategies.",
     "Authentication subsystem comparison."),

    (19, "Authentication II: JWT Verification, Claims, Expiry & Public Key (RS256)",
     "Cryptographic tokens: symmetric HMAC (HS256) vs asymmetric RSA/ECDSA (RS256) key pairs.",
     ["JWT structure: Header, Payload (Claims), Signature", "Standard claims: `sub` (subject), `exp` (expiration), `iat` (issued at), `iss` (issuer)", "Symmetric signing (HS256): single secret key shared between auth and API services", "Asymmetric signing (RS256): auth service signs with private key; API gateways verify with public key"],
     "Failing to enforce the `alg` header check, allowing attackers to forge tokens using `alg: 'none'` bypasses.",
     "Generate an RSA keypair, issue signed RS256 tokens, and verify token signatures in a standalone FastAPI service.",
     "Secure enterprise JWT authentication microservice."),

    (20, "Role-Based Access Control (RBAC) & Scope Enforcement",
     "Authorization architectures: users, roles, permissions, and granular OAuth2 scopes in APIs.",
     ["Authentication vs Authorization: 'Who are you?' vs 'What are you allowed to do?'", "RBAC models: User -> Role (Admin, Editor, Viewer) -> Permission", "FastAPI Security scopes: `@app.get('/', dependencies=[Security(check_perms, scopes=['prompts:write'])])`", "Tenant boundary isolation: preventing cross-tenant data access"],
     "Performing authorization checks inside the UI while omitting backend role verification on mutation endpoints.",
     "Implement an automated RBAC dependency that validates user scopes and prevents unauthorized data mutation.",
     "Multi-tenant RBAC engine."),

    (21, "Token Bucket & Leaky Bucket Rate Limiting with Redis",
     "Protecting APIs from abuse: rate-limiting algorithms, atomic Redis operations, and sliding windows.",
     ["Fixed window rate limiting: the double-burst edge case", "Token bucket algorithm: steady state consumption with burst allowances", "Leaky bucket: smoothing bursty traffic into uniform output rate", "Distributed rate limiting with Redis: atomic Lua scripts to prevent race conditions"],
     "Implementing rate limits with multiple non-atomic Redis `GET` and `SET` calls, causing race conditions under load.",
     "Write an atomic Redis Lua script implementing the token bucket algorithm that rejects requests over budget.",
     "GatewayAI: DDoS and rate limiting defense."),

    (22, "Server-Sent Events (SSE): Streaming Tokens & EventSource Protocol",
     "Real-time one-way streaming: HTTP persistent connections, text/event-stream, and chunked transfer encoding.",
     ["SSE vs WebSockets: why one-way HTTP streaming is optimal for LLM token delivery", "The `text/event-stream` MIME format: `event: ...`, `data: ...`, `id: ...`, double newlines (`\\n\\n`)", "FastAPI `StreamingResponse`: yielding async generator token chunks without buffering", "Automatic browser reconnection: the `Last-Event-ID` header and resume semantics"],
     "Buffering entire generator outputs in memory before responding, breaking real-time token streaming to clients.",
     "Build an SSE endpoint in FastAPI that streams token chunks to an EventSource client with 50ms latency.",
     "GatewayAI: Real-time LLM token streaming proxy."),

    (23, "Streaming LLM Responses: Chunking, Backpressure & Flush Delays",
     "Production AI streaming: handling network backpressure, reverse proxy buffering, and client disconnects.",
     ["Reverse proxy buffering traps: why Nginx buffers SSE unless `X-Accel-Buffering: no` is set", "Client disconnection detection: cancelling expensive LLM inference when the user closes their browser tab", "Token chunking strategies: word boundary buffering vs character streaming", "Backpressure handling: preventing fast LLM token generation from overwhelming slow mobile network clients"],
     "Continuing to consume expensive OpenAI/Anthropic API tokens after a client disconnects.",
     "Implement client disconnect detection (`request.is_disconnected()`) in an async generator loop to halt inference immediately.",
     "GatewayAI: Cost-saving streaming cancellation."),

    (24, "WebSocket Protocol: Full-Duplex Bidirectional Frame Communication",
     "Upgrading HTTP to persistent TCP duplex connections: handshake, frames, and ping/pong keepalives.",
     ["WebSocket handshake: `Connection: Upgrade` and `Sec-WebSocket-Key`", "Framing protocol: text frames, binary frames, close frames, and masking", "FastAPI WebSocket endpoint: `websocket.accept()`, `send_text()`, `receive_text()`", "Handling abrupt disconnections, network drops, and socket cleanup"],
     "Failing to handle client disconnect exceptions, leaking orphaned WebSocket connections and memory.",
     "Build a WebSocket echo and chat server in FastAPI with robust connection error handling.",
     "Real-time interactive canvas and agent communication."),

    (25, "Real-Time Pub/Sub with WebSockets & Redis Channels",
     "Scaling WebSockets horizontally: multi-server connection broadcasting with Redis Pub/Sub.",
     ["The multi-server problem: client A is on Server 1, client B is on Server 2", "Redis Pub/Sub architecture: publish to topic, subscribers receive instantaneous broadcast", "Connection manager pattern: tracking active WebSockets per room or tenant", "Fanout performance: broadcasting messages to 10,000 clients with minimal latency"],
     "Storing active WebSocket connections in a single server's local RAM when running behind a multi-pod load balancer.",
     "Implement a multi-worker Redis Pub/Sub message broker delivering updates across multiple FastAPI instances.",
     "Distributed collaborative workspace backend."),

    (26, "Background Tasks & Asynchronous Job Offloading in FastAPI",
     "Non-blocking responses: offloading slow operations with FastAPI `BackgroundTasks` vs external Celery/ARQ workers.",
     ["FastAPI native `BackgroundTasks`: running post-response functions inside the event loop", "When native background tasks fail: process crashes losing in-memory task state", "Distributed task queues (Celery, ARQ, Celery/Redis): durability, retries, and worker pools", "Task status polling vs Webhooks: notifying clients when long jobs finish"],
     "Running a 2-minute batch image generation task inside a standard HTTP request thread, timing out at 30 seconds.",
     "Schedule an asynchronous background email and metric dispatch using FastAPI BackgroundTasks.",
     "Async audit and telemetry logging."),

    (27, "Asynchronous HTTP Clients with HTTPX: Connection Pooling & Keep-Alive",
     "High-performance external API communication: HTTP connection reuse, pooling, and async timeouts.",
     ["`httpx.AsyncClient` lifecycle: why creating a new client per request destroys socket performance", "TCP connection reuse: avoiding 3-way handshakes and TLS renegotiation via HTTP Keep-Alive", "Pool tuning: `max_connections` and `max_keepalive_connections` limits", "Fine-grained timeouts: connect timeout vs read timeout vs pool acquisition timeout"],
     "Instantiating `httpx.AsyncClient()` inside an endpoint function on every request, exhausting OS ephemeral ports.",
     "Create a singleton HTTPX client dependency with tuned connection pools and verify socket reuse across requests.",
     "GatewayAI: High-throughput upstream LLM client."),

    (28, "Resilient API Integration: Exponential Backoff, Jitter & Circuit Breakers",
     "Building bulletproof third-party integrations: handling rate limits (429), timeouts, and cascading failures.",
     ["Exponential backoff: doubling wait intervals ($2^n$) to allow remote services to recover", "Full Jitter: adding randomized noise to prevent the 'thundering herd' synchronization problem", "Circuit Breaker pattern: Closed (normal), Open (fast-fail without calling remote), Half-Open (test probe)", "Fallbacks: returning cached results or degraded responses during complete provider outages"],
     "Retrying failed API calls without jitter, causing thousands of synchronized clients to hammer an already-failing service.",
     "Implement a circuit breaker with exponential backoff and jitter for upstream AI provider calls.",
     "Production resilience for AI API gateways."),

    (29, "File Uploads: Streaming Multi-Part Form Data & Spooling",
     "Handling large file ingest: avoiding RAM exhaustion with streaming multipart forms and spooling files.",
     ["`multipart/form-data` MIME framing: boundaries, parts, headers, and content", "FastAPI `UploadFile` vs `bytes`: why `bytes` crashes servers on 1GB uploads", "Python `SpooledTemporaryFile`: storing small files in RAM and spilling to disk above thresholds", "Direct-to-S3 signed upload URLs: bypassing the application server entirely for multi-gigabyte files"],
     "Reading an entire 500MB user-uploaded PDF into memory as `await file.read()`, triggering Linux OOM kills.",
     "Build a streaming file upload handler that processes multi-megabyte files in 64KB chunks with constant memory usage.",
     "DocuMind: Document ingestion pipeline."),

    (30, "OpenAPI / Swagger Specification Generation & Schema Drift Prevention",
     "Contracts between frontend and backend: OpenAPI v3 specification generation, Swagger UI, and automated client generation.",
     ["OpenAPI v3 JSON schema generated dynamically from FastAPI route types and Pydantic models", "Swagger UI (`/docs`) and ReDoc (`/redoc`) interactive documentation endpoints", "Auto-generating TypeScript types from the OpenAPI schema using `openapi-typescript`", "Preventing schema drift: CI/CD checks that fail builds when frontend types and backend schemas desync"],
     "Manually maintaining frontend TypeScript interfaces that slowly desync from backend API models.",
     "Export an OpenAPI schema from FastAPI and auto-generate type-safe TypeScript client interfaces.",
     "Frontend/Backend contract synchronization."),

    (31, "API Versioning Strategies: URL Path vs Header vs Query Parameter",
     "Evolving production APIs without breaking clients: versioning models and deprecation lifecycles.",
     ["URL path versioning: `/api/v1/prompts` vs `/api/v2/prompts` (explicit, visible, cache-friendly)", "Header versioning: `Accept: application/vnd.company.v2+json` (clean URIs, more complex client setup)", "Query parameter versioning: `/prompts?version=2`", "Sunset policies and `Sunset` HTTP headers: giving API consumers advance notice of deprecations"],
     "Introducing a breaking field rename in a production API without versioning, crashing all mobile and frontend clients.",
     "Implement a versioned API router that routes requests to v1 or v2 handlers with deprecation headers.",
     "Enterprise API lifecycle management."),

    (32, "High-Performance Caching: HTTP Cache-Control, ETag & 304 Not Modified",
     "Eliminating redundant server work: HTTP caching headers, conditional requests, and validation.",
     ["Cache-Control directives: `public`, `private`, `no-cache`, `no-store`, `max-age`, `s-maxage`", "Validation with ETags (entity tags) and `If-None-Match` headers", "HTTP 304 Not Modified: returning empty bodies when cached client copies are still fresh", "CDN edge caching: configuring Cloudflare/CloudFront to cache static and semi-static API responses"],
     "Setting `Cache-Control: public, max-age=3600` on endpoints returning personalized user profile data.",
     "Implement ETag generation for an API endpoint and return 304 Not Modified on matching client caches.",
     "API response caching and CDN optimization."),

    (33, "Structured JSON Logging, Log Correlation & Contextual Tracing",
     "Production debugging: replacing `print()` statements with structured JSON logs and correlation contexts.",
     ["Why plaintext logs fail at scale: impossible to query across millions of entries in Datadog/Elastic", "Structured JSON format: `{\"timestamp\": ..., \"level\": \"INFO\", \"request_id\": ..., \"message\": ...}`", "Python `structlog` and standard `logging` formatters", "Context variables (`contextvars`): propagating correlation IDs through asynchronous call chains"],
     "Logging sensitive user passwords, API keys, or JWT tokens in plaintext application logs.",
     "Configure a structured JSON logger that automatically attaches request IDs to all log statements in an async call.",
     "Production debugging and log observability."),

    (34, "Load Testing & Performance Benchmarking with Locust and wrk",
     "Measuring API breaking points: throughput (RPS), p50/p95/p99 latency percentiles, and concurrency limits.",
     ["Why averages lie: the importance of p95 and p99 latency tail distributions", "HTTP benchmarking tools: `wrk` for high-throughput pipeline testing vs `locust` for realistic user flows", "Identifying bottlenecks: CPU-bound serialization vs database lock contention vs socket exhaustion", "Establishing Service Level Objectives (SLOs): 99.9% of requests under 150ms latency"],
     "Relying on local development performance without load testing, experiencing 10-second outages on launch day.",
     "Benchmark a FastAPI endpoint using wrk to determine its maximum requests per second and p99 latency.",
     "GatewayAI: High-load performance certification."),

    (35, "Deploying High-Availability Web Services with Docker & Health Checks",
     "Containerizing production APIs: multi-stage Dockerfiles, non-root users, and liveness/readiness probes.",
     ["Multi-stage Docker builds: compiling dependencies in builder stage and copying to a minimal slim image", "Running as unprivileged non-root users (`USER appuser`) for container security", "Liveness probes (`/health/live`) vs Readiness probes (`/health/ready`): preventing traffic to unready pods", "Graceful shutdown handling: catching SIGTERM, waiting for in-flight requests to complete, and closing connections"],
     "Using a bloated 1.2GB full Python Docker image in production instead of a hardened 120MB slim image.",
     "Write a production multi-stage Dockerfile with non-root security and comprehensive health check endpoints.",
     "GatewayAI: Cloud-ready containerized service deployment.")
]

print(f"Loaded {len(P4_LESSONS)} modern web architecture lessons.")
